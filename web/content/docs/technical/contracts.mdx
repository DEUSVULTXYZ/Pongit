<Callout title="Testnet preview">Classic and Chaos rooms use test MON. Live multiplayer, verified event draws and automatic payments have been exercised on the hosted engine and Monad. Hosted availability and rate limits still apply. V4 balances and financial features remain separate.</Callout>

## Rooms deployment

The active manifest identifies the immutable multi-room application on Monad Testnet 10143, with separate Classic and Chaos ratings inherited from the published preceding deployment. Classic keeps its physics. Chaos adds 24 verified random events, with two simultaneous effects at most. Rules-6 markets use continuous betting and the `events-v1` settlement adapter, including late-bet refunds. The rules-8 candidate requires a new game, adapter, sealed vault and market; existing addresses retain their withdrawals. Human admissions remain closed during recovery and qualification. Rules 8 is not live until its new manifest and qualification are published.

The [rooms manifest](https://github.com/DEUSVULTXYZ/Pongit/blob/main/deployments/interlude-rooms.json) identifies the current engine. Its [deployment evidence](https://github.com/DEUSVULTXYZ/Pongit/blob/main/deployments/chaos-events.json) lists the stateless modules and deployment transactions. Original markets and their immutable financial bindings remain in the [financial manifest](https://github.com/DEUSVULTXYZ/Pongit/blob/main/deployments/rooms-finance.json). The tables below list the preserved V1 to V4 contracts.

## Preserved V4 arcade

V4 remains available separately. V4 deploys new immutable GameV3, ArcadeSessions and Vault instances with MarketV4 and TournamentsV4. The implementation name GameV3 is not the same as the application's deployment generation.

The following tables are generated from the repository's public active manifest and its legacy chain. They expose only version, chain, start block and contract addresses. Follow each address to the testnet explorer.

<ContractTable />

## Bindings and upgrades

Game, market, vault and tournament links are sealed before use. These contracts have no upgradeable proxy. A new deployment may inherit ratings, but existing balances and financial rights remain attached to the old contracts.

The treasury is immutable where specified by the contract constructor. Replacing it requires a compatible new deployment, not an undocumented administrative edit to an existing match.

## Match references

A rooms reference includes base chain, application address, delegation epoch and full match identifier. A bare number or previous rooms URL must never silently resolve to another deployment.

`v4:12` means match 12 in V4. It is different from `v3:12`. Envio and private replay notes use deployment-qualified references. Active numeric API routes are scoped by `/api/config`; legacy reads require the chosen deployment parameter.

Code must preserve the deployment when storing a receipt, replay, position or payout origin. Never assume a number alone identifies a match across upgrades.

## Financial compatibility

V4 transfers due winnings and refunds to the fixed wallet recipient. V1–V3 claims credit their original vaults and need a signed withdrawal. A UI rollback must retain support for every deployment already accepted by the relayer journal.

The [source manifest](https://github.com/DEUSVULTXYZ/Pongit/blob/main/deployments/testnet.json) includes deployment evidence. The [permission model](/docs/technical/permissions) and [operations guide](/docs/technical/operations) explain the boundaries around these contracts.
